import React from "react";

const Title = ({ title }) => {
  return (
    <div className="card-title">
      <h1 className="text-white text-center p1">{title}</h1>
    </div>
  );
};

export default Title;
